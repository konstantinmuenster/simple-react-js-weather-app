import React from 'react';

import classes from './Date.module.css';

const date = (props) => {
    return(
        <div className={classes.DateWrapper}>
            Friday, June 09
        </div>
    );
}

export default date;